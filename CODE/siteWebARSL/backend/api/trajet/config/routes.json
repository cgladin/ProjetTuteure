{
  "routes": [
    {
      "method": "GET",
      "path": "/trajets",
      "handler": "Trajet.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/trajets/count",
      "handler": "Trajet.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/trajets/:id",
      "handler": "Trajet.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/trajets",
      "handler": "Trajet.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/trajets/:id",
      "handler": "Trajet.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/trajets/:id",
      "handler": "Trajet.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
